# MCP server is no longer the primary AI path.
# Direct Anthropic API calls are used instead (see lib/claude.ts).
# ANTHROPIC_API_KEY is set in .env.local at the app root.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
