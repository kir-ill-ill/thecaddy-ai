name: CaddyAI
options:
  bundleIdPrefix: com.caddyai
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
    INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
    INFOPLIST_KEY_UILaunchScreen_Generation: YES
    INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
    INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
    SWIFT_EMIT_LOC_STRINGS: YES
    GENERATE_INFOPLIST_FILE: YES
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: 1.0
    PRODUCT_BUNDLE_IDENTIFIER: com.caddyai.app

targets:
  CaddyAI:
    type: application
    platform: iOS
    sources:
      - path: CaddyAI
        excludes:
          - "**/.DS_Store"
      - path: CaddyAI/Resources/Assets.xcassets
        type: folder
    settings:
      base:
        INFOPLIST_KEY_CFBundleDisplayName: CaddyAI
        PRODUCT_NAME: CaddyAI
    scheme:
      testTargets:
        - CaddyAITests
      gatherCoverageData: true

  CaddyAITests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: CaddyAITests
        optional: true
    dependencies:
      - target: CaddyAI
    settings:
      base:
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/CaddyAI.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/CaddyAI"
